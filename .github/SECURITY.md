# Security and responsible disclosure

Do not open a public issue containing DHS records, credentials, direct controlled-data links, local paths, or row-level predictions. Report a suspected secret or sensitive-data exposure privately to the repository owner and revoke the exposed credential immediately.

This demo intentionally has no authentication because it serves a synthetic model only. Add authentication, rate limiting, privacy-preserving logs, and an approval process before any controlled-data service is considered.
