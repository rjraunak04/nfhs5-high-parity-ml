# Data directory

Only `demo_batch.csv` may be committed here. It is generated by `scripts/train_demo_model.py` from deterministic synthetic records.

Keep approved DHS files outside the repository and pass their absolute path to `scripts/train_research_model.py`. The `.gitignore` blocks common survey-data formats, but you must still review `git status` before pushing.
